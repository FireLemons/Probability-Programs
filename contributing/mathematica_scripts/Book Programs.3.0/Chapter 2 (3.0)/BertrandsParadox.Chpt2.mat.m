(***********************************************************************
This file was generated automatically by the Mathematica front end.
It contains Initialization cells from a Notebook file, which typically
will have the same name as this file except ending in ".nb" instead of
".m".

This file is intended to be loaded into the Mathematica kernel using
the package loading commands Get or Needs.  Doing so is equivalent to
using the Evaluate Initialiation Cells menu command in the front end.

DO NOT EDIT THIS FILE.  This entire file is regenerated automatically 
each time the parent Notebook file is saved in the Mathematica front end.
Any changes you make to this file will be overwritten.
***********************************************************************)

Clear[BertrandsParadox];
BertrandsParadox[n_, print_] :=
Block[{s, t1, t2, q, theta, x0, x1, y0, y1, 
       r, l2, alpha, l3,
       list1 = {}, list2 = {}, list3 = {},
       total1 = 0, total2 = 0, total3 = 0
      },
(*  circ produces the circle on which the experiments are displayed *)
      circ=Graphics[Circle[{0,0},1]];
      For[i = 1, i <= n , i++,
          s = True;
          While[s,
                t1 = Random[Real, {-1,1}];
                t2 = Random[Real, {-1,1}];
                If[(t1^2)+(t2^2) <= 1, s = False]
               ];
          If[(Mod[i, 100] == 0)&&(print),
             q = Sqrt[(t1^2)+(t2^2)];
             If[Random[Integer, {0,1}] == 0,
                theta = ArcTan[t2/t1],
                theta = ArcTan[t2/t1] + Pi//N
               ];
             x0 = Cos[theta - ArcCos[q]];
             x1 = Cos[theta + ArcCos[q]];
             y0 = Sin[theta - ArcCos[q]];
             y1 = Sin[theta + ArcCos[q]];
             list1 = Append[list1, 
                            Graphics[Line[{{x0, y0},
                                           {x1, y1}
                                          }
                                         ]
                                    ]
                           ]
            ];
          l1 = 2*Sqrt[1-((t1^2)+(t2^2))];
          If[l1 > Sqrt[3]//N, total1++];
          
          r = Random[Real, {-1,1}];
          If[(Mod[i, 100] == 0)&&(print),
             list2 = Append[list2, 
                            Graphics[Line[{{Sqrt[1-(r^2)],r},
                                           {-Sqrt[1-(r^2)],r}
                                          }
                                         ]
                                    ]
                           ]
            ];
          l2 = 2*Sqrt[1-(r^2)];
          If[l2 > Sqrt[3]//N, total2++];
          
          alpha = Random[Real, {0, 2*Pi//N}];
          If[(Mod[i, 100] == 0)&&(print),
             list3 = Append[list3, 
                            Graphics[Line[{{1,0},
                                           {Cos[alpha], Sin[alpha]}
                                          }
                                         ]
                                    ]
                           ]
            ];
          l3 = Sqrt[2 - (2*Cos[alpha])];
          If[l3 > Sqrt[3]//N, total3++]
         ];
      Print["Case 1:  Probability (chord length > Sqrt[3]) = ", total1/n//N];
      Print["Case 2:  Probability (chord length > Sqrt[3]) = ", total2/n//N];
      Print["Case 3:  Probability (chord length > Sqrt[3]) = ", total3/n//N];
      If[print,
         Show[circ, list1, AspectRatio->.95, PlotLabel->"Case 1"];
         Show[circ, list2, AspectRatio->.95, PlotLabel->"Case 2"];
         Show[circ, list3, AspectRatio->.95, PlotLabel->"Case 3"]
        ]
     ]